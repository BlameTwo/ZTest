<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="clr-namespace:SimpleUI.Controls">
    <Style x:Key="NavigationViewHeaderBase" TargetType="{x:Type ui:NavigationViewHeader}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ui:NavigationViewHeader}">
                    <Grid>
                        <TextBlock
                            Margin="10,0"
                            HorizontalAlignment="Left"
                            FontSize="15"
                            FontWeight="Bold"
                            Text="{TemplateBinding Header}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>