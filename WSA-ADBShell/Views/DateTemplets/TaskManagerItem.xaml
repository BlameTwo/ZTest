<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ui="clr-namespace:SimpleUI.Controls;assembly=SimpleUI"
    xmlns:vm="clr-namespace:WSA_ADBShell.ViewModels.ItemsViewModels">
    <DataTemplate x:Key="DefaultTaskItemTemplate" DataType="{x:Type vm:TaskManagerItemVM}">
        <Border
            Margin="3"
            Padding="5"
            Background="{DynamicResource Default.ContextMenu.Background.Brush}"
            BorderBrush="{DynamicResource Default.ContextMenu.BorderBrush.Brush}"
            BorderThickness="1"
            CornerRadius="5"
            IsEnabled="{Binding IsRun}">
            <Border.ContextMenu>
                <ContextMenu>
                    <MenuItem
                        Command="{Binding RemoveMeCommand}"
                        Header="移除任务"
                        Icon="&#xE160;" />
                </ContextMenu>
            </Border.ContextMenu>
            <Grid ToolTipService.ToolTip="{Binding NowOutput, Mode=OneWay}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>
                <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
                    <TextBlock Text="{Binding Title}" />
                    <TextBlock Text="---" />
                    <TextBlock Text="{Binding SubTitle}" />
                </StackPanel>
                <ui:Button
                    Grid.Column="1"
                    HorizontalAlignment="Right"
                    Command="{Binding StartCommand}"
                    Content="{Binding StringState}"
                    Icon="Play" />
            </Grid>
        </Border>
    </DataTemplate>
</ResourceDictionary>